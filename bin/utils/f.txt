  if (nombre.contains('stark') && nombre.contains('eddard'))
    return '"El invierno se acerca." — Eddard Stark';
  else if (nombre.contains('lannister') && nombre.contains('tywin'))
    return '"Un Lannister siempre paga sus deudas." — Tywin Lannister';
  else if (nombre.contains('targaryen') && nombre.contains('daenerys'))
    return '"No soy una reina ordinaria. Soy la Madre de Dragones." — Daenerys Targaryen';
  else if (nombre.contains('baratheon') && nombre.contains('robert'))
    return '"Ours is the fury." — Robert Baratheon';
  else if (nombre.contains('greyjoy') && nombre.contains('balon'))
    return '"Lo que está muerto no puede morir." — Balon Greyjoy';
  else if (nombre.contains('tyrell') && nombre.contains('margaery'))
    return '"Crecer es lo único que hacemos." — Margaery Tyrell';
  else if (nombre.contains('arryn') && nombre.contains('jon'))
    return '"Alto como el honor." — Jon Arryn';
  else if (nombre.contains('bolton') && nombre.contains('roose'))
    return '"Un hombre despellejado envía un mensaje." — Roose Bolton';
  else if (nombre.contains('melisandre'))
    return '"La noche es oscura y alberga horrores." — Melisandre';
  else if (nombre.contains('bran'))
    return '"No hay uno solo de nosotros que no haya pecado." — Bran Stark';
  else
    return '';
}
